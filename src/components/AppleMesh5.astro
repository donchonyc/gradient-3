---
import astroLogo from '../assets/astro.svg';
---

<div class="container">
  <div class="gradient-background">
    <div class="wave-gradient">
      <div class="wave wave1"></div>
      <div class="wave wave2"></div>
      <div class="wave wave3"></div>
      <div class="light-spots"></div>
    </div>
  </div>
  
  <main>
    <p class="korean-copy">
      믿고 몰입할 수 있는 학습 경험을 설계합니다.
    </p>
    <p class="korean-subtitle">
      우리는 인공지능과 자연어 처리 기술을 바탕으로, 인간적인 언어 학습의 가능성을 넓히고 누구나 쉽게 접근할 수 있는 앱과 웹 서비스를 개발합니다.
    </p>
    
    <!-- Add Join Us CTA -->
    <div class="cta-container">
      <a href="/career" class="join-us-cta">
        <span class="cta-text">조인하기</span>
        <span class="cta-icon">
          <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <path d="M5 12h14"></path>
            <path d="m12 5 7 7-7 7"></path>
          </svg>
        </span>
      </a>
    </div>
  </main>
</div>

<style>
  @import url(//spoqa.github.io/spoqa-han-sans/css/SpoqaHanSansNeo.css);

  /* Reset and base styles */
  * {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
  }

  :root {
    --primary: #4d21fc;
    --secondary: #7828c8;
    --accent: #f41cf4;
    --background: #F8FAFC;
    --text: #000000;
    --text-light: #ffffff;
    --text-muted: rgba(0, 0, 0, 0.9);
    --text-secondary: rgba(0, 0, 0, 0.75);
    --radius: 12px;
    --shadow: 0 8px 30px rgba(0, 0, 0, 0.1);
    --wave-color-1: #A6D5FF;
    --wave-color-2: #FFD1A6;
    --wave-color-3: #A7F3D0;
  }

  /* Container layout with responsive height */
  .container {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    min-height: 650px;
    padding: 16px;
    position: relative;
    overflow: hidden;
  }

  /* Wave gradient background */
  .gradient-background {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    z-index: -1;
    background-color: #F8FAFC;
    overflow: hidden;
  }

  .wave-gradient {
    position: absolute;
    inset: 0;
    pointer-events: none;
    filter: saturate(1.05);
    will-change: transform;
    overflow: hidden;
  }

  .wave {
    position: absolute;
    width: 200%;
    height: 200%;
    opacity: 0.7;
    border-radius: 40%;
  }

  .wave1 {
    background: var(--wave-color-1);
    left: -50%;
    bottom: -125%;
    animation: wave-rotate1 20s linear infinite;
  }

  .wave2 {
    background: var(--wave-color-2);
    left: -40%;
    bottom: -115%;
    animation: wave-rotate2 17s linear infinite reverse;
  }

  .wave3 {
    background: var(--wave-color-3);
    left: -60%;
    bottom: -105%;
    animation: wave-rotate3 15s linear infinite;
  }

  .light-spots {
    position: absolute;
    inset: 0;
    background: 
      radial-gradient(circle at 20% 30%, rgba(166, 213, 255, 0.4) 0%, transparent 30%),
      radial-gradient(circle at 80% 20%, rgba(167, 243, 208, 0.4) 0%, transparent 30%),
      radial-gradient(circle at 65% 70%, rgba(255, 209, 166, 0.4) 0%, transparent 30%);
    mix-blend-mode: soft-light;
    filter: blur(20px);
  }

  @keyframes wave-rotate1 {
    from {
      transform: rotate(0deg);
    }
    to {
      transform: rotate(360deg);
    }
  }

  @keyframes wave-rotate2 {
    from {
      transform: rotate(0deg);
    }
    to {
      transform: rotate(360deg);
    }
  }

  @keyframes wave-rotate3 {
    from {
      transform: rotate(0deg);
    }
    to {
      transform: rotate(360deg);
    }
  }

  /* Add soft noise texture */
  .wave-gradient::after {
    content: '';
    position: absolute;
    inset: 0;
    background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 200 200' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noise'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.75' numOctaves='2' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noise)' opacity='0.03'/%3E%3C/svg%3E");
    pointer-events: none;
    z-index: 2;
    mix-blend-mode: overlay;
  }

  .korean-copy {
    font-size: clamp(28px, 5.5vw, 42px);
    line-height: 1.5;
    text-align: center;
    font-weight: 600;
    max-width: 900px;
    margin: 0 auto;
    padding: 0 20px;
    color: var(--text-muted);
    font-family: 'Spoqa Han Sans Neo', system-ui, sans-serif;
    word-break: keep-all;
    letter-spacing: -0.02em;
    text-shadow: 0 1px 1px rgba(255, 255, 255, 0.8);
    margin-bottom: 1.5rem;
  }

  .korean-subtitle {
    font-size: clamp(16px, 2.5vw, 20px);
    line-height: 1.7;
    text-align: center;
    font-weight: 400;
    max-width: 760px;
    margin: 0 auto;
    padding: 0 20px;
    color: var(--text-secondary);
    font-family: 'Spoqa Han Sans Neo', system-ui, sans-serif;
    word-break: keep-all;
    letter-spacing: -0.01em;
  }

  .highlight {
    color: #000;
    position: relative;
    font-weight: 700;
  }

  /* Position the text */
  main {
    position: relative;
    z-index: 3;
    margin-bottom: 0;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
  }

  /* Responsive adjustments */
  @media (min-width: 768px) {
    /* Tablet */
    .container {
      min-height: 500px;
    }
    
    .korean-copy {
      font-size: clamp(32px, 6vw, 48px);
      max-width: 900px;
      line-height: 1.4;
    }
    
    .korean-subtitle {
      font-size: clamp(18px, 2.8vw, 22px);
      max-width: 700px;
      line-height: 1.6;
    }
  }

  @media (min-width: 1024px) {
    /* Desktop */
    .container {
      min-height: 100vh;
    }
    
    .korean-copy {
      font-size: clamp(38px, 7vw, 58px);
      max-width: 1100px;
      line-height: 1.35;
    }
    
    .korean-subtitle {
      font-size: clamp(20px, 3vw, 24px);
      max-width: 840px;
      line-height: 1.6;
    }
  }

  @media (max-width: 480px) {
    /* Small mobile */
    .container {
      min-height: 450px;
    }
    
    .korean-copy {
      font-size: clamp(24px, 5vw, 32px);
      line-height: 1.6;
      padding: 0 15px;
      margin-bottom: 1rem;
    }
    
    .korean-subtitle {
      font-size: clamp(14px, 2.5vw, 18px);
      line-height: 1.7;
      padding: 0 15px;
    }

    .wave1 {
      animation-duration: 15s;
    }

    .wave2 {
      animation-duration: 12s;
    }

    .wave3 {
      animation-duration: 10s;
    }
  }

  /* New CTA styles */
  .cta-container {
    margin-top: 2.5rem;
    display: flex;
    justify-content: center;
  }
  
  .join-us-cta {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 10px;
    padding: 14px 30px;
    background: linear-gradient(135deg, var(--wave-color-1) 0%, var(--wave-color-3) 100%);
    border-radius: 50px;
    color: rgba(0, 0, 0, 0.8);
    font-family: 'Spoqa Han Sans Neo', system-ui, sans-serif;
    font-size: 18px;
    font-weight: 600;
    text-decoration: none;
    box-shadow: 0 10px 20px rgba(166, 213, 255, 0.3);
    transition: all 0.3s ease;
    position: relative;
    overflow: hidden;
  }
  
  .join-us-cta::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: linear-gradient(135deg, rgba(255, 255, 255, 0.2) 0%, rgba(255, 255, 255, 0) 100%);
    opacity: 0;
    transition: opacity 0.3s ease;
  }
  
  .join-us-cta:hover {
    transform: translateY(-3px);
    box-shadow: 0 15px 30px rgba(166, 213, 255, 0.4);
  }
  
  .join-us-cta:hover::before {
    opacity: 1;
  }
  
  .cta-text {
    position: relative;
    z-index: 2;
    letter-spacing: 0.5px;
  }
  
  .cta-icon {
    display: flex;
    align-items: center;
    justify-content: center;
    position: relative;
    z-index: 2;
    transition: transform 0.3s ease;
  }
  
  .join-us-cta:hover .cta-icon {
    transform: translateX(3px);
  }
  
  /* Responsive adjustments for CTA */
  @media (max-width: 768px) {
    .cta-container {
      margin-top: 2rem;
    }
    
    .join-us-cta {
      padding: 12px 24px;
      font-size: 16px;
    }
    
    .cta-icon svg {
      width: 18px;
      height: 18px;
    }
  }
  
  @media (max-width: 480px) {
    .cta-container {
      margin-top: 1.75rem;
    }
    
    .join-us-cta {
      padding: 10px 20px;
      font-size: 15px;
    }
    
    .cta-icon svg {
      width: 16px;
      height: 16px;
    }
  }
</style>
