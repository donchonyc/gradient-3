<section class="work-apeiro">
  <div class="container">
    <p class="subtitle">Apeiro에서 근무하기</p>
    <h1 class="gradient-heading">팀에 참여하고 변화를 만들어보세요.</h1>
    
    <div class="cta-container">
      <a href="https://apeirocorp.notion.site/Career-d6a18b8da56b401787827fde601ac8bf" class="cta-button">채용 공고 보기</a>
    </div>
  </div>
</section>

<style>
  @import url(//spoqa.github.io/spoqa-han-sans/css/SpoqaHanSansNeo.css);

  /* Black background section */
  .work-apeiro {
    background-color: #000;
    color: #fff;
    padding: 8rem 1rem;
    position: relative;
    overflow: hidden;
  }

  .container {
    max-width: 1200px;
    margin: 0 auto;
    text-align: center;
  }

  .subtitle {
    font-size: clamp(1.5rem, 3.5vw, 2.2rem);
    font-weight: 500;
    text-align: center;
    color: #A6D5FF;
    margin-bottom: 1.5rem;
    letter-spacing: -0.01em;
    font-family: 'Spoqa Han Sans Neo', system-ui, sans-serif;
    display: block;
    width: 100%;
    opacity: 0.9;
    position: relative;
  }
  
  .gradient-heading {
    font-size: clamp(2.5rem, 5vw, 4rem);
    font-weight: 700;
    text-align: center;
    margin-bottom: 2.5rem;
    line-height: 1.3;
    letter-spacing: -0.02em;
    font-family: 'Spoqa Han Sans Neo', system-ui, sans-serif;
    background: linear-gradient(
      135deg,
      #8a4fff 0%,
      #6c4aff 25%,
      #4969f9 50%,
      #4aa1f9 75%,
      #4ac9f9 100%
    );
    background-size: 200% auto;
    color: transparent;
    -webkit-background-clip: text;
    background-clip: text;
    animation: gradient-shift 8s ease infinite;
  }
  
  @keyframes gradient-shift {
    0% {
      background-position: 0% 50%;
    }
    50% {
      background-position: 100% 50%;
    }
    100% {
      background-position: 0% 50%;
    }
  }
  
  /* CTA Button Styles */
  .cta-container {
    margin: 2rem 0 1rem;
    text-align: center;
  }
  
  .cta-button {
    display: inline-block;
    background: linear-gradient(135deg, #4d21fc 0%, #7828c8 100%);
    color: white;
    font-size: 1.2rem;
    font-weight: 600;
    text-decoration: none;
    padding: 1rem 2.5rem;
    border-radius: 50px;
    box-shadow: 0 4px 20px rgba(77, 33, 252, 0.5);
    transition: all 0.3s ease;
  }
  
  .cta-button:hover {
    transform: translateY(-2px);
    box-shadow: 0 6px 25px rgba(77, 33, 252, 0.6);
  }
  
  .cta-button:active {
    transform: translateY(0);
    box-shadow: 0 2px 10px rgba(77, 33, 252, 0.5);
  }
  
  /* Responsive styles */
  @media (max-width: 768px) {
    .work-apeiro {
      padding: 6rem 1rem;
    }
    
    .cta-button {
      font-size: 1.1rem;
      padding: 0.9rem 2rem;
    }
    
    .subtitle {
      font-size: clamp(1.3rem, 3vw, 1.6rem);
      margin-bottom: 1.2rem;
    }
  }
</style>