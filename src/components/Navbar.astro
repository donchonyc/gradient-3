---
// Navbar component for Apeiro
---

<nav class="navbar">
  <div class="navbar-container">
    <div class="logo">
      <a href="/">Apeiro</a>
    </div>
    <div class="nav-links">
      <a href="/career" class="nav-link">커리어</a>
      <a href="/company">회사소개</a>
    </div>
  </div>
</nav>

<style>
  /* Import Lexend font from Google Fonts */
  @import url('https://fonts.googleapis.com/css2?family=Lexend:wght@400;500;600;700&display=swap');

  .navbar {
    display: flex;
    width: 100%;
    height: 44px; /* Apple.com standard navbar height */
    background-color: rgba(255, 255, 255, 0.8); /* Slightly transparent like Apple */
    backdrop-filter: saturate(180%) blur(20px); /* Apple's signature blur effect */
    z-index: 9999; /* Ensure navbar stays on top */
  }

  .navbar-container {
    display: flex;
    width: 100%;
    max-width: 1280px; /* Reduced maximum width */
    padding: 0 24px; /* Reduced horizontal padding */
    justify-content: space-between;
    align-items: center;
    margin: 0 auto;
    box-sizing: border-box; /* Ensure padding is included in width calculations */
  }

  .logo a {
    font-family: 'Lexend', sans-serif;
    font-weight: 600;
    font-size: 20px;
    color: #000;
    text-decoration: none;
  }

  .nav-links {
    display: flex;
    align-items: center;
    gap: 40px; /* Reduced gap between links */
  }

  .nav-links a {
    font-family: 'Lexend', sans-serif;
    color: #000;
    text-decoration: none;
    font-size: 14px;
    font-weight: 400;
    white-space: nowrap;
  }

  .nav-links a:hover {
    opacity: 0.8;
  }

  /* Responsive adjustments */
  @media (max-width: 768px) {
    .navbar-container {
      padding: 0 16px;
    }
    
    .nav-links {
      gap: 24px;
    }
  }
</style>